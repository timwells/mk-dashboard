const _version = "1.0.1";
const state = {
  version: _version
};

const getters = {};
const mutations = {};
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
